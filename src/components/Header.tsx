import React, { FC } from "react";

const Header: FC = () => {
    return (
        <nav className="bg-header text-center text-white text-2xl py-5 px-2 font-serif w-full">
            Contact Page
        </nav>
    );
};

export default Header;
